Having the library registered, set the `URL` from where it will be downloaded at build time, and explicitly fetch it.
Also set a variable for the library version.
Get the latest version number from the [library website](http://nuclear.mutantstargoat.com/sw/kdtree/).
